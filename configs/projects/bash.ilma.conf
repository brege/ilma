# Configuration for bash/shell script projects

RSYNC_EXCLUDES+=(
    --exclude '*.tmp'
    --exclude '*.temp'
    --exclude 'tmp/'
    --exclude 'temp/'
    --exclude '.DS_Store'
    --exclude 'Thumbs.db'
    --exclude '*.swp'
    --exclude '*.swo'
    --exclude '*~'
    --exclude '.*.un~'
)
TREE_EXCLUDES+="|tmp|temp|backup"
