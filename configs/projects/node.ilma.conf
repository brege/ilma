# Node.js project configuration
EXTENSIONS=(js ts jsx tsx cjs mjs md json yaml)
CREATE_COMPRESSED_ARCHIVE=false

RSYNC_EXCLUDES+=(
    --exclude 'node_modules/'
    --exclude 'package-lock.json'
    --exclude 'dist/'
    --exclude 'build/'
    --exclude '.next/'
    --exclude 'coverage/'
)
TREE_EXCLUDES+="|node_modules|package-lock.json|dist|build|.next|coverage"
